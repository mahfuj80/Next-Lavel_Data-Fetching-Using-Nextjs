import { NextResponse } from "next/server";

// Get API
export const GET = () => {
  return NextResponse.json({ message: "Hello, world!" });
};

// Post API

export const POST = () => {
  return NextResponse.json({ message: "Hello, world!" });
};

// Put API

export const PUT = () => {
  return NextResponse.json({ message: "Hello, world!" });
};

// Delete API

export const DELETE = () => {
  return NextResponse.json({ message: "Hello, world!" });
};

// Patch API
export const PATCH = () => {
  return NextResponse.json({ message: "Hello, world!" });
};
